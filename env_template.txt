# Скопируйте этот файл в .env и заполните своими данными

# Данные для авторизации на LMS Synergy
LOGIN=your_email@example.com
PASSWORD=your_password

# Данные для Gologin
# Если используете Gologin API
GOLOGIN_API_TOKEN=your_gologin_api_token
GOLOGIN_PROFILE_ID=your_profile_id

# Если используете локальный профиль Gologin
# GOLOGIN_PROFILE_PATH=C:\path\to\gologin\profile

# Настройки парсера
MIN_VIEW_TIME=30
SCROLL_DELAY=2
PAGE_LOAD_TIMEOUT=30

# Камера/микрофон (нужно для идентификации перед тестами)
# 1 = включено (как в старом проекте), 0 = выключено
ALLOW_MEDIA_STREAM=1

# Настройки для тестов
TEST_STRATEGY=random
MAX_TEST_ATTEMPTS=3

# AI (для TEST_STRATEGY=ai)
# Выберите провайдера (пока реализован grok)
AI_PROVIDER=grok
AI_TIMEOUT_SECONDS=60

# Grok (xAI)
# ВАЖНО: ключ НЕ коммитьте в git. Храните только в .env.
GROK_API_KEY=your_grok_api_key
GROK_MODEL=grok-4-1-fast-reasoning
GROK_BASE_URL=https://api.x.ai/v1

# Логирование
LOG_LEVEL=INFO
LOG_FILE=logs/parser.log

# Артефакты/отладка (опционально)
DEBUG_HTML_DIR=artifacts/debug_html
DEBUG_NDJSON_LOG=logs/agent_debug.ndjson

# ===== Django backend (admin + API) =====
# Секрет Django (обязательно поменяйте в проде)
DJANGO_SECRET_KEY=dev-insecure-secret-key-change-me
# 1 = DEBUG, 0 = production-like
DJANGO_DEBUG=1
# Можно указать список хостов через запятую (в проде не ставьте "*")
DJANGO_ALLOWED_HOSTS=*

# Postgres (рекомендуется). Если не задано — будет sqlite в backend/db.sqlite3
# Пример: postgres://user:pass@localhost:5432/synergy
DATABASE_URL=

# CORS (для отдельного фронта/бота)
CORS_ALLOW_ALL_ORIGINS=1

# Celery/Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Ключ шифрования для Synergy-паролей студентов (обязательно в проде!)
# Должен быть стабильным и секретным. Например base64-строка.
CREDENTIAL_ENCRYPTION_KEY=

# ===== Stripe (оплата) =====
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Куда Stripe редиректит после оплаты/отмены (можно на сайт/страницу бота)
STRIPE_SUCCESS_URL=http://localhost:8000/admin/
STRIPE_CANCEL_URL=http://localhost:8000/admin/
